export const config = {
    showFPS: true,
    // debug: true,
    referWidth: 375,
    dpi: 2,
    canvasId: 'tinyView',
    renderOptions: {
        // 这里颜色要run一个stage出来才会有
        backgroundColor: 0x1abc9c,
    },
    renderer: 'webgl'
}
